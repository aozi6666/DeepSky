export default function ButtonPage() {
  return (
    <div style={{ textAlign: 'left' }}>
      <h1 style={{ marginTop: 0 }}>Button</h1>
      <p>这是 Button 页面，用来验证 MainLayout + Outlet 的路由渲染。</p>
    </div>
  )
}

